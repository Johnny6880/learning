name: 03-Workflow Runners

on: push

jobs: 
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps:
            - name: show OS
              run: |
                echo "this job is running on ubuntu runner"
                echo "My runner OS is ${RUNNER_OS}"